"""Worker package.

Houses the background processing pipeline: task orchestration, FFmpeg helpers,
transcription, AI moment selection, metadata generation, captioning, and visual
effects. All modules are stubs at this stage.
"""
